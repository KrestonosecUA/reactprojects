import React from 'react';

const Comment = ({comment}) => {

    const {id, postId, name, email, bode} = comment

    console.log(comment)

    return (
        <div>
            
        </div>
    );
};

export default Comment;